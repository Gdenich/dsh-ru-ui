# Extracted English inventories and composed translations are reproducible from
# the installed DSH build; keep the repo to sources plus the shipped bundle.
build/

# macOS
.DS_Store
__pycache__/
*.pyc
node_modules/
dist/

# Local-only helper and notes: kept in the working tree, never published.
# The recipes are specific to one maintainer's account and workflow.
docs/publishing-recipe.md
tools/preflight.sh
